const numerator = 10;

const DENOMINATOR = 2;

const answer = numerator / DENOMINATOR; // 5
console.log(answer);

let incrementer = 1;

let start = incrementer; // 1

let end;

let difference;

incrementer += 1;
incrementer += 1;
incrementer += 1;

incrementer++;
incrementer++; // 6

end = incrementer; // 6

difference = end - start; // 5

console.log(end);
console.log(difference);

const answer2 = (11 + 31) * 3 // 126
console.log(answer2);